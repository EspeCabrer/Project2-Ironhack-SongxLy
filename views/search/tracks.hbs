<div class= "totalAlbums">
    <div class="row">
        {{#each tracksArr}}
             <div class="name">{{this.name}}</div>
             <audio controls src="{{this.preview_url}}"></audio>      
             <a href="/lyric/{{this.artists.0.name}}/{{this.name}}">Ver letra</a>   
        {{/each}}
    </div>
</div>