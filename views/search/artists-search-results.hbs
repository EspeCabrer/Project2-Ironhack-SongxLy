
<div class= "totalArtists card">
    <div class="row">
        {{#each totalArtistsArr}}
             <div class="name">{{this.name}}</div>
             <img class="img" src="{{this.images.[0].url}}" alt="{{this.name}}">
             <a href="/albums/{{this.id}}">Álbum</a>         
        {{/each}}
    </div>
</div>