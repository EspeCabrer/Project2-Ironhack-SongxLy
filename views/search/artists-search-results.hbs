

<div class= "artists-page">
    <h2>Resultados de: "{{artistsNameSearch}}"</h2>
    <div class= "totalArtists row row-cols-1 row-cols-md-4 g-4 text-center m-3">
        {{#each totalArtistsArr}}
         <div class= "col">
            <div class="card border-info border-3 mb-3 bg-light">
                <div class= "card-header text-dark">
                    <h5>{{this.name}}</h5>
                </div>
                 <img class="img" src="{{this.images.[0].url}}" alt="{{this.name}}">
                 <a class =" btn btn-light" href="/albums/{{this.id}}">Ver álbumes</a>  
            </div>
        </div>
        {{/each}}
    </div>
</div>